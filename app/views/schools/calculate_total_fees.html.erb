<%= form_tag(@school, method: :get, remote: false) do |f|%>
	   <%= label_tag "First child" %>
	   <%= select_tag(:class_id, options_for_select(CurrentClass.all.map{ |f| [f.current, f.id]}),:selected => params[:class_id], :prompt => "Select Class") %>
	   <%= label_tag "Second child" %>
	   <%= select_tag(:class_id_2, options_for_select(CurrentClass.all.map{ |f| [f.current, f.id]}),:selected => params[:class_id_2], :prompt => "Select Class") %>
		<%= submit_tag "Calculate", class: "calculate_fees"%>
	<% end %>

<!-- 
It's going to cost you <%= @current_fees %> to send your kid to this school
<script type = "text/javascript">
$(".calculate_fees").on("click", function(){
    $.ajax({
        url: "schools/calculate_total_fees/id=" + <%= @school.id %>,
        data: <%= @current_fees %>,
        success: function(data) {
            alert(data)
        }
    });
});
</script>  -->